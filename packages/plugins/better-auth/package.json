{
	"name": "@euroclaw/better-auth",
	"version": "0.0.0",
	"type": "module",
	"description": "The better-auth integration across all three access concerns — identity, membership (org + role), authz — each taking only the function it needs, plus a betterAuthAccess({ auth }) bundle.",
	"license": "MIT",
	"author": "Konstantin Ponomarev",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"files": [
		"dist",
		"README.md"
	],
	"scripts": {
		"build": "rm -rf dist && tsc",
		"test": "vitest run src",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@euroclaw/policy-better-auth": "workspace:*",
		"@euroclaw/policy-core": "workspace:*"
	}
}
